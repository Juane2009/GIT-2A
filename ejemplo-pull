este es un conjunto de como agregar o atualizar datos desde git hub y verlos en vs-code
para realizar esto utilizamos el comando
git pull origin master
o si trabajas en la rama main
git pull origin main
